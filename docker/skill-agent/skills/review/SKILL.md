---
name: backlite-review
description: Stub. Slice 5 (#49) will populate this skill with PR-review instructions.
---

# Backlite review skill (stub)

This skill is a placeholder. It exists so the skill-agent image can build
with the directory layout the entrypoint expects. The real instructions land
in Slice 5 (issue #49).

For now, an agent dispatched here should write a failure `status.json` with
`error: "review skill not yet implemented (issue #49)"` and exit non-zero.
